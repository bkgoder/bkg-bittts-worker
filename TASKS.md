# Tasks

## Current

- `w.0.1`: Fix `BITTTS_BUNDLE_FORCE=1` so remote workers can refresh the trainer bundle after coordinator-side engine fixes.

## Later

- Add supervisor integration after the trainer supervisor repo exists.
